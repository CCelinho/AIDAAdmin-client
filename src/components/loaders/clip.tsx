import { ClipLoader } from 'react-spinners';

const Clip = () => {
  return (
    <div className="flex h-screen items-center justify-center">
      <ClipLoader size={150} color="#22d3ee" />
    </div>
  );
};

export default Clip;
